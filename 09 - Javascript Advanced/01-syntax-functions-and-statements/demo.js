//The Largest Number
function findMaxNumber (first, second, third) {

    let maxNumer = Math.max(first, second, third);
    console.log(`The largest number is ${maxNumer}.`);
}

findMaxNumber(5, -3, 16)